# AGENTS.md — TaczHacker

## 项目概述

这是一个 **Minecraft 1.20.1 Forge 空模组**（基于 Forge MDK 模板初始化，目前只有模板示例代码），
目标是给 **Tacz（Timeless and Classics Zero）** 枪械模组实现一套**"挂"（作弊功能）**，**仅用于娱乐**。

- 平台：Minecraft 1.20.1 + Forge
- 依赖：需要接入 Tacz 枪械模组（当前 `build.gradle` **尚未**声明任何 Tacz 依赖，见"与 Tacz 的集成"）
- 定位：以**纯客户端（CLIENT ONLY）** 为主，服务器不装本 mod 即可使用大部分功能；
  部分功能（追踪弹、穿墙子弹）**需要服务端也装本 mod**（或单机/局域网）才能生效
- **可配置性要求（作者要求）**：所有功能开关与参数必须在 **Forge 配置 UI**（mods 列表 → 本 mod → "Config"）中可调，
  所有按键必须是注册的 `KeyMapping`（原版"控制/按键绑定"里可改）——**任何开关/数值不得硬编码**
- 目标环境：**无任何反作弊的原版式 Forge 服务器**（作者确认），因此旋转/移动包可被自由利用，无封号风险
- 遇到任何不确定的问题（版本、实现可行性、功能细节偏好），**直接询问项目作者（用户）**

> ⚠️ **发包频率纪律（作者明确要求）**：原版服务器对高频发包有检测，**不能发太多包**。
> 所有功能必须优先采用**零额外发包**设计——尽量复用原版已有的移动/旋转包；
> 确需主动发包时，频率压到极低（如开火瞬间 1 个包、转圈刷新 ≤ 每 10 tick 一次）并做成配置项。
> 任何"每 tick 主动发包"的设计都是**禁止的**。

> ⚠️ **互斥规则**：功能 1（开火静默自瞄）、功能 2（低头转圈）、功能 3（视角锁定）都涉及"服务器看到的旋转"，
> 需要定义优先级：**功能 1/功能 3（玩家主动开火/按键）> 功能 2（转圈）**——转圈在开火那一 tick 暂停，发瞄准旋转。

> **与 Tacz 的集成**：保证在不修改Tacz的情况下就可以运行本mod
> 保证用户mod+原生Tacz+本mod情况下可以运行游戏能正常工作两个mod的功能
> 仅要求玩家再游戏环境可以运行本mod+tacz，不必须要求生产环境

> **用户问答过程中**：```gradle build```等指令由你来执行，检查bug报错，修改。
> 如果用户说有问题，你可以选择查看日志，日志路径在下文给出。
> 每次结束后，你应该帮我运行```gradle build```，检查是否有错误，如果有，日志路径在```"D:\MC\.minecraft\versions\1.20.1TaczHacktst\logs\debug.log"```。否则，帮我把build/libs的产物复制到```"D:\MC\.minecraft\versions\1.20.1TaczHacktst\mods"```目录下。

> **配置/mod配置/mod配置ui菜单**：指的是forge+cloth config api实现的配置菜单（mods列表 → 本mod → "配置"）

> **玩家日志/游戏/mods路径**：日志在```"D:\MC\.minecraft\versions\1.20.1TaczHacktst\logs\debug.log"```，不要尝试在项目根目录寻找

## 技术栈与环境

| 项 | 值 |
|---|---|
| Minecraft | 1.20.1（`minecraft_version_range=[1.20.1,1.21)`） |
| Forge | 47.4.23 |
| 映射 | official（Mojang 官方名） |
| Java | 17 |
| 构建 | ForgeGradle `[6.0.16,6.2)` |
| modid | `taczhacker` |
| group / 包 | `com.qw` / `com.qw.taczhacker` |
| 主类 | `com.qw.taczhacker.Taczhacker`（`@Mod(Taczhacker.MODID)`） |

常用命令：

```bash
gradlew build          # 构建 mod jar（含 reobfJar）
gradlew runClient      # 启动客户端调试
gradlew runData        # 数据生成（一般用不到）
```

gradle在:
```
[gradle.bat](gradle-8.8/bin/gradle.bat)
"E:\MCMod\Tacz_Hacker\gradle-8.8\bin\gradle.bat"
```

## 目录结构

```
src/main/java/com/qw/taczhacker/
├── Taczhacker.java    # 主类（含模板遗留的示例方块/物品/物品栏标签，可清理）
├── Config.java        # ForgeConfigSpec 配置（当前为模板示例，将扩展为"挂"的配置中心）
src/main/resources/
├── META-INF/mods.toml # mod 元数据
└── pack.mcmeta
```
- 注：项目根目录下存在原生tacz 模组，用于测试与验证。

建议新增的子包（按此组织，功能互相解耦）：

```
com.qw.taczhacker.config    # 所有配置项（集中管理）
com.qw.taczhacker.keybind   # 按键绑定（KeyMapping）
com.qw.taczhacker.mixin     # Mixin 相关
com.qw.taczhacker.feature   # 各功能实现
    ├── aim        # 功能 1：开火静默自瞄（+ 追踪弹/穿墙子弹附加，需服务端或单机）
    ├── fakerot    # 功能 2：低头转圈
    ├── aimbot     # 功能 3：视角锁定自瞄
    ├── xray       # 功能 4：透视（半透明模式）
    └── flight     # 功能 5：飞行挂
```

一个路径，似乎是forge：```C:\Users\DIY\.gradle\caches\forge_gradle\minecraft_user_repo\net\minecraftforge\forge\1.20.1-47.4.23_mapped_official_1.20.1```


## 功能需求

以下为作者提出的五个功能。每个功能给出**表现**、**配置项**、**实现思路**、**待确认点**与**可用性标注**。

### 部署环境与可用性总览

两种部署模式：
- **仅客户端模式**：只有客户端装了本 mod（服务器只装原生 Tacz）——只能开启"仅客户端即可"的功能
- **服务端支持模式**：单机 / 局域网，或服务端与客户端**都装了本 mod**——额外可开启"需要服务端（或单机）"的功能
  （服务端 mixin 能作用到权威子弹实体；整合服同理）

| 功能 | 可用性 | 说明 |
|---|---|---|
| 功能 1 开火静默自瞄（主体） | **仅客户端即可** | 服务器无需装本 mod，利用"服务器信任客户端旋转" |
| 功能 1 · 真·追踪弹（附加） | **需要服务端（或单机）** | 需修改服务器权威子弹的运动 |
| 功能 1 · 穿墙子弹（附加） | **需要服务端（或单机）** | 需修改服务器权威子弹的碰撞判定 |
| 功能 2 低头转圈 | **仅客户端即可** | 纯发包旋转欺骗 |
| 功能 3 视角锁定自瞄 | **仅客户端即可** | 纯本地视角修改 |
| 功能 4 透视 X-ray | **仅客户端即可** | 纯本地渲染 |
| 功能 5 飞行挂 | **仅客户端即可** | 纯本地移动，经原版移动包上传 |

> 实现要求：客户端启动/进服后检测**服务端支持状态**（单机 = 支持；联机 = 通过自定义握手包/通道检测服务器是否装有本 mod），
> 并根据状态决定哪些功能可开启；不可用的功能在配置中禁用或标注（见"配置设计"）。

---

### 功能 1：开火静默自瞄（silent aim，主方案）——【仅客户端即可】

**表现**：
- **本地**：视角与移动完全正常，照常走路、瞄准，无任何被"接管"的感觉
- **服务器视角**：开火瞬间"看到"你快速转身朝目标开了一枪，子弹朝目标飞去并**能打中**
- 打完一发后，服务器上你的朝向由原版周期移动包自然校正回真实朝向（无需恢复包）

**实现思路（客户端，每次开火仅 1 个额外包）**：
1. 检测**开火瞬间**：Mixin Tacz 客户端射击逻辑（`GunFireController` 或对应开火点，以实际版本反编译为准）
2. 开火前选择目标：按全局配置过滤（目标类型 / 半径 / 锥角 / 可选穿透视线），选最优（最近/最接近弹道方向）
3. **提前量预测**：用目标速度 × 子弹飞行时间（距离/弹速）计算预测落点，再算瞄准 yaw/pitch——对移动目标命中率远高于直瞄当前位置，观感最接近"追踪"
4. 直接构造 `ServerboundMovePlayerPacket.Rot`（假旋转）发送，**不改本地 `player.xRot/yRot` 字段** → 本地视角无感
5. Tacz 开火包随后发出；服务器按顺序先更新旋转（"看到转身"）再生成子弹 → 子弹朝目标飞
6. **不发送恢复包**：原版 `LocalPlayer#sendPosition()` 每 20 tick 的周期包会自然校正服务器旋转
7. 优先级：开火那一 tick 功能 2（转圈）暂停，发瞄准旋转；功能 3 按住时与功能 1 不冲突（本来就在瞄准）

**配置项**：
- 总开关
- 目标类型（复用全局：Player / Mob / 指定实体列表）
- 锁定半径、追踪锥角（只瞄准星附近目标还是全向）
- 提前量系数（0 = 直瞄当前位置；建议默认 1.0）
- 是否穿透障碍物（忽略视线遮挡）——仅影响"选目标"，**不影响子弹是否撞墙**
- 穿墙子弹开关（默认关闭，注释注明"需要服务端（或单机）"）
- 是否仅持枪时生效

**附加方案（需要服务端支持）——【需要服务端（或单机）】**：
- **真·追踪弹（homing bullet）【需要服务端（或单机）】**：Mixin `TimelessBulletEntity#tick()`，持续把子弹 `deltaMovement` 转向目标（子弹中途拐弯）
- **穿墙子弹（无视方块碰撞）【需要服务端（或单机）】**：Mixin `TimelessBulletEntity#tick()` 的命中判定，**跳过方块碰撞分支**（或把穿透格数拉满/设为无限），子弹穿过墙体命中目标
- **生效条件**：单机 / 局域网（整合服同 JVM，mixin 作用到权威子弹）；或**真实服务器也装了本 mod**（服务端 mixin 作用到权威子弹）
- **仅客户端时无效**：命中判定在服务器端，客户端改 motion / 碰撞逻辑只是本地渲染假象；服务器上"能瞄墙后的人"与"子弹能穿墙"是脱节的（子弹仍会撞墙，除非 Tacz 弹药自带穿透属性）
- "追踪 + 穿墙 + 透视"联动体验完整；实现时以实际版本反编译确认 `TimelessBulletEntity` 的命中/碰撞代码点

**待确认（问作者 / 反编译验证）**：
- Tacz 开火时子弹方向的来源：① 服务器按玩家当前旋转计算（→ 走上述旋转包方案）② 客户端开火包自带方向（→ 直接改包内方向，**更省包**，甚至零额外包）
- 以实际接入的 Tacz 版本反编译结果为准，两种都能纯客户端完成

---

### 功能 2：低头转圈（fake rotation）——【仅客户端即可】

**表现**：在**他人视角**中，你的角色**低着头**（pitch 接近 90°）并且**持续转圈**（yaw 不断旋转）；
**你自己的视角完全正常**，可以正常走路、瞄准、射击。

**实现思路（客户端，零额外发包优先）**：
- 核心原则：**只骗服务器/他人，不骗自己**
- 首选：Mixin `LocalPlayer#sendPosition()`，**直接替换原版移动包里携带的旋转**为假旋转（低头 + yaw 每 tick 递增）
  ——完全复用原版发包节奏，**零额外发包**
- 副作用：静止站立时原版约每 20 tick 发一次位置包，别人看到的转圈会"一顿一顿"（约 1Hz）；
  可配置**主动刷新间隔**（如每 10 tick 主动发一个旋转包 = 2 包/秒，仍远低于风险线；默认关闭或很低）
- 备选：Mixin `Entity#getViewYRot/getViewXRot`（1.20.1 渲染用视角 getter，以反编译为准），注意不要影响自己的第一人称相机
- 实现时确认：1.20.1 下 `ServerboundMovePlayerPacket` 的旋转取自哪个 getter，选最稳的 hook 点

**配置项**：
- 开关
- 低头角度（默认 ~90）
- 旋转速度（度/tick）
- 主动刷新间隔（tick，0 = 仅随原版包，不主动发）
- 是否仅持枪时生效

**互斥规则**：功能 1 开火瞬间 / 功能 3 按住时优先于转圈。

---

### 功能 3：视角锁定自瞄（aimbot / lock-on）——【仅客户端即可】

**表现**：绑定一个**指定按键**，**按住**时玩家视角自动**平滑锁定到附近目标头部**。
可选：**透过障碍物瞄准**（无视视线遮挡）。

**实现思路（客户端，零额外发包）**：
- 注册 `KeyMapping`（客户端按键），按住时每个 tick：
  1. 收集候选目标：附近 `LivingEntity`（可配置范围、目标类型）
  2. 若关闭"穿透障碍物"：用射线/视线检查过滤被墙挡住的目标
  3. 选最优目标（最近 / 最接近准星）
  4. 计算指向目标头部（眼睛位置 + 头高，如 `getEyeY() + 0.1`）的 yaw/pitch
  5. **平滑插值**（lerp）后写入 `player.setYRot()/setXRot()`
- 本地旋转经**原版移动包**自然上传给服务器，**零额外发包**
- 松开按键后停止干预，恢复正常视角

**配置项**：
- 按键（默认键位与作者确认，建议用不冲突的键）
- 锁定范围、目标类型（复用全局配置）
- 平滑度 / 锁定速度
- 是否穿透障碍物
- 锁头 vs 锁身体

---

### 功能 4：透视 X-ray（半透明模式）——【仅客户端即可】

**表现**：开启后给**方块表面叠加一层很浅的半透明颜色**——地形轮廓隐约可见（**防止掉坑**），
而矿石等目标方块透过地形清晰可见（高亮）；可选用经典"全隐藏"模式。

**实现思路（客户端）**：
- **主方案（半透明模式）**：方块渲染时叠加浅色半透明覆盖（改顶点 alpha / 半透明 RenderType / shader），
  人眼能看透地形，同时保留地形轮廓避免踩空掉坑；目标方块（矿石等）用高亮/全彩显示
  ——具体 hook 点（`LevelRenderer` / `ModelBlockRenderer` / shader）需在 1.20.1 下实验确定最稳方案
- **备选（经典模式）**：非目标方块完全不渲染（老式 x-ray），注意此模式看不见地面、容易掉坑
- **实体透视（ESP）**（可选）：Forge `RenderLevelStageEvent` 绘制方框/名称标签，穿过墙体可见

**配置项**：
- 总开关
- 模式：半透明（默认）| 经典全隐藏
- 覆盖颜色、透明度（默认很浅）
- 目标方块列表（`ResourceLocation` 列表，如 `minecraft:diamond_ore`、`minecraft:iron_ore`…）
- 目标方块高亮开关
- 是否启用实体 ESP

---

### 功能 5：飞行挂（fly hack）——【仅客户端即可】

**表现**：按住按键/开关飞行——水平向视线方向移动、垂直自由升降；正常走路视角。
无反作弊服务器下安全（作者确认无反作弊）；配置注释注明"有反作弊的服务器有风险"。

**实现思路（客户端，零额外发包）**：
- 每 tick 给玩家加**小步增量速度**（水平向视线方向、垂直升降），位置经**原版移动包**上传 → 零额外发包
- ⚠️ **不要**单 tick 瞬移/大位移：会触发原版内置 "player moved wrongly"（位置偏差检测，非反作弊）被踢；
  用**速度累加**而非直接改坐标，单 tick 位移控制在安全范围（如 ≤ 数格）
- 原版服务器不校验"是否允许飞行"，纯客户端修改即可生效

**配置项**：
- 开关
- 按键
- 水平速度（格/tick 上限）
- 垂直速度
- 是否按住键才飞（vs 开关切换）

---

## 配置设计（Config.java 扩展）

- 把"挂"的所有配置放进一个 **客户端配置**（`ModConfig.Type.CLIENT`），与模板示例的 COMMON 配置分开或替换
- **游戏内配置 UI（作者要求）**：所有配置项必须能在游戏内 Mod 配置界面调整——用 `ConfigGuiHandler.ConfigGuiFactory`
  注册客户端配置界面（CLIENT 侧，mods 列表 → 本 mod → "Config" 按钮进入）；界面可用 Cloth Config（需额外依赖）
  或自行构建简单屏幕，把每个 `ForgeConfigSpec` 项映射成可编辑控件；**确保每个功能参数都能在 UI 里改**
- **目标类型配置统一设计**，供功能 1 / 3 复用：`目标类型 = PLAYER | MOB | 自定义实体列表`（枚举 + 附加实体 id 列表）
- 每个配置项加中文注释，说明用途与取值范围
- **可用性标注约定（作者要求）**：每个功能/配置项必须标注可用性——
  - **仅客户端即可**：服务器不装本 mod 也能用
  - **需要服务端（或单机）**：必须单机/局域网，或服务端也装本 mod 才能用
  标注同时写入生成的 `.toml` 配置描述注释，避免作者在服务器上误以为功能失效是 bug
- **运行时可用性检测**：客户端启动/进服后检测服务端支持状态（单机 = 支持；联机 = 自定义握手包/通道检测），
  服务端不支持的功能在配置中禁用或提示，而不是直接报错
- **发包频率相关配置**（转圈主动刷新间隔等）单独归组，注释说明频率与风险
- 配置变更通过 `ModConfigEvent` 刷新缓存（参考现有 `Config.java` 的 `onLoad` 模式）

## 与 Tacz 的集成

**Tacz jar 已就位**：`tacz-1.20.1-1.1.8-hotfix.jar`，位于**项目根目录**（当前目录下）。
当前 `build.gradle` 的 `dependencies` 为空，接入时以该本地 jar 为依赖（flatDir 指向项目根目录，或移入 `libs/` 后 `implementation fg.deobf(...)`）。
**注意：作者网络可能无法访问 GitHub**，外部文档仅作参考，**以该 jar 反编译为准**。

接入方式（按可用性排序）：
1. **本地 jar（已确认）**：`tacz-1.20.1-1.1.8-hotfix.jar`（项目根目录），`implementation fg.deobf(...)`（flatDir 仓库）——不依赖网络，最可靠
2. **CurseMaven**（备选）：`curse.maven:tacz-480288:<fileId>`（项目 id 以实际 CurseForge 页面为准；CurseForge 不一定可访问）
3. **官方 maven**（备选，若网络可达）：参考官方文档（可能打不开）：
   - https://github.com/MCModderAnchor/TACZ/blob/1.20.1/readme.md
   - https://deepwiki.com/machinacanis/TACZ/2-project-setup

**关键类（以实际版本反编译为准）**：
- 子弹实体：`com.tacz.guns.entity.TimelessBulletEntity`（功能 1 单机追踪弹 / 穿墙子弹的 mixin 目标）
- 客户端射击逻辑（`GunFireController` 或对应开火点）：功能 1 静默自瞄的 hook 点

**待确认清单（问作者 / 反编译验证）**：
- 开火时子弹方向来源：① 服务器按玩家旋转计算（旋转包方案）还是 ② 客户端开火包自带方向（改包内方向方案）
- 服务器 Tacz 版本与客户端一致（客户端为 1.1.8-hotfix）

## 通用技术要点

- **以客户端实现为主**：客户端事件订阅用 `@Mod.EventBusSubscriber(modid, bus = MOD, value = Dist.CLIENT)` 或
  `MinecraftForge.EVENT_BUS` 上的客户端事件；按键用 `net.minecraft.client.KeyMapping`（`FMLClientSetupEvent` 注册，
  或用 `KeyMappingHandler` 监听）
- **全部可配置（作者要求）**：每个功能的总开关与参数都来自 `ForgeConfigSpec` 并映射进游戏内配置 UI；
  每个按键都是注册的 `KeyMapping`（归入统一分类，如 `TaczHacker`，在原版"控制"里可改）。**禁止硬编码任何开关/数值**
- **服务端支持检测**：进服后检测服务端是否装有本 mod（自定义握手包/通道），单机视为支持；
  根据检测结果决定可开启的功能（见"配置设计"）
- **Mixin**：1.20.1 Forge 下需要引入 Mixin（`mixinConfig` 在 `mods.toml` 声明；若用 MixinBooter 注意与 Forge 47 兼容）。
  Mixin 是改子弹、改旋转、改渲染的主力手段，**先把 Mixin 环境跑通再写功能**
- **需要服务端的功能**（追踪弹/穿墙）：同一套 mixin 在服务端（装有本 mod）同样加载生效；对权威实体的修改
  （如 `TimelessBulletEntity`）必须用 `level.isClientSide` 判断只在服务端逻辑生效——客户端影子实体上的改动只是本地渲染假象
- **发包频率纪律（红线）**：禁止任何"每 tick 主动发包"的设计；优先复用原版包（`sendPosition` mixin 等）；
  主动发包仅限：开火瞬间 1 个旋转包、转圈可配置低频刷新（默认 ≤ 每 10 tick 一次）
- **渲染**：`RenderLevelStageEvent`（Forge 事件）用于 ESP / 透视绘制
- 服务端（装有本 mod 时）只承担必要的权威逻辑修改（子弹追踪/穿墙），不添加独立功能模块
- 所有功能都有**总开关**，默认关闭或谨慎默认，便于测试

## 代码约定

- 包结构见上文"目录结构"，每个功能一个子包，尽量解耦
- 用户可见文本、配置注释用**中文**
- 沿用模板风格：Java 17、日志用 slf4j `LOGGER`、`DeferredRegister` 注册
- 模板遗留的示例方块/物品/物品栏标签（`EXAMPLE_BLOCK` 等）与功能无关，可清理

## 沟通

作者明确表示"**有什么问题可以找我**"，且**网络可能无法访问 GitHub**。出现以下情况时，先向作者确认再动手：

1. Tacz 版本与 jar（**已确认**：`tacz-1.20.1-1.1.8-hotfix.jar` 在项目根目录）——如更换版本需告知
2. 目标运行环境确认（无反作弊的 Forge 服务器，Tacz 版本一致）
3. 关键方案的可行性（如 Tacz 射击方向来源、mixin hook 点选哪个）
4. 功能细节偏好（默认按键、参数默认值、外观表现、优先级规则）
